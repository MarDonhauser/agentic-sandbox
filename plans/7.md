# Plan for #7: Add deployment probes and a second replica

## Goal
Run two application replicas and use `/health` readiness and liveness probes so traffic is sent only to healthy pods and unhealthy pods are restarted.

## Dependencies
- confirmed: `k8s/deployment.yaml` currently has one replica and no probes; change: yes
- confirmed: `src/app.ts` serves `GET /health` on the HTTP port; change: no
- confirmed: `k8s/service.yaml` targets the named `http` port; change: no

## Tests
- Pipeline rollout, stability, smoke, and browser checks validate both replicas and probe behavior.

## Environment contract
No environment variables are added or changed.

## Risks
Incorrect probe path or port can prevent rollout; overly aggressive timings could cause restarts. The pipeline must confirm rollout, both pods ready, stability, smoke, and browser checks.

## Open questions
None.

## Decisions
Use the existing named `http` port and `/health` endpoint for both probes.
